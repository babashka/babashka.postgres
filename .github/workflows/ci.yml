name: CI

on: [push, pull_request]

jobs:
  bb:
    name: babashka (${{ matrix.os }})
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-2022]
    runs-on: ${{ matrix.os }}
    defaults:
      run:
        shell: bash
    steps:
      - uses: actions/checkout@v4

      - uses: ikalnytskyi/action-setup-postgres@v8
        if: runner.os != 'Windows'
        id: postgres

      # The Windows runner ships PostgreSQL; the action would download and
      # extract an installer instead, which takes minutes
      - name: Start the preinstalled PostgreSQL
        if: runner.os == 'Windows'
        run: |
          sc.exe config postgresql-x64-14 start= demand
          net start postgresql-x64-14
          echo "$PGBIN" >> "$GITHUB_PATH"

      - name: Install babashka dev build
        run: |
          bash <(curl -s https://raw.githubusercontent.com/babashka/babashka/master/install) --dev-build --dir . --dynamic

      - name: Run tests
        env:
          PG_TEST_CONNINFO: ${{ steps.postgres.outputs.connection-uri || 'postgresql://postgres:root@localhost:5432/postgres' }}
        run: |
          if [ "$RUNNER_OS" = "Windows" ]; then bb=./bb.exe; else bb=./bb; fi
          "$bb" test:bb

  jvm:
    name: JVM (${{ matrix.os }})
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-2022]
    runs-on: ${{ matrix.os }}
    defaults:
      run:
        shell: bash
    steps:
      - uses: actions/checkout@v4

      - uses: ikalnytskyi/action-setup-postgres@v8
        if: runner.os != 'Windows'
        id: postgres

      # The Windows runner ships PostgreSQL; the action would download and
      # extract an installer instead, which takes minutes
      - name: Start the preinstalled PostgreSQL
        if: runner.os == 'Windows'
        run: |
          sc.exe config postgresql-x64-14 start= demand
          net start postgresql-x64-14
          echo "$PGBIN" >> "$GITHUB_PATH"

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '25'

      - uses: DeLaGuardo/setup-clojure@13.4
        with:
          cli: latest

      - name: Run tests
        env:
          PG_TEST_CONNINFO: ${{ steps.postgres.outputs.connection-uri || 'postgresql://postgres:root@localhost:5432/postgres' }}
        run: clojure -M:test
